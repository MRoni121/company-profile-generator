import React from 'react';

const ProfileGenerator: React.FC = () => {
    return (
        <div>
            <h1>Profile Generator</h1>
            <p>This is the Profile Generator component.</p>
        </div>
    );
};

export default ProfileGenerator;
